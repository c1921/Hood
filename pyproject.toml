[project]
name = "hood"
version = "0.1.0"
description = "AI 工作流编排后端 — 将 RunningHub 云端推理与本地 ComfyUI VAE 解码串联为 HTTP API"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "requests>=2.32",
    "websocket-client>=1.9.0",
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
]
